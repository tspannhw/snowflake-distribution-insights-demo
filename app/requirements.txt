# MQTT → Snowpipe Streaming V2 ingest pipeline
# Install: pip install -r requirements.txt

# MQTT client (v2 callback API)
paho-mqtt>=2.1.0

# Snowflake connector for Python (primary ingest path — keypair auth works reliably)
# Docs: https://docs.snowflake.com/en/developer-guide/python-connector/python-connector-connect
snowflake-connector-python>=3.0.0

# Snowpipe Streaming V2 SDK (kept for future use when SDK bug is fixed)
# Bug: SDK Rust core uppercases org-based account identifiers in JWT iss claim,
# causing HTTP 401 (error_code=390144) on Snowflake SSv2 endpoints.
# Track: https://github.com/snowflakedb/snowflake-ingest-python
snowpipe-streaming>=0.2.0

# Environment / config
python-dotenv>=1.1.0

# Synthetic data generation
faker>=37.0.0

# RSA key loading (used by both consumer and diagnostic scripts)
cryptography>=43.0.0
